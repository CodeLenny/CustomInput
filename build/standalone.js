// Generated by CoffeeScript 1.8.0
(function() {
  var baseUrl, checkCallback, github, loadGlobally, repo, version;

  github = true;

  baseUrl = "";

  if (github) {
    repo = "CodeLenny";
    version = "0.1";
    baseUrl = "//cdn.rawgit.com/" + repo + "/CustomInput/" + version + "/build/";
  } else {
    baseUrl = "";
  }

  loadGlobally = true;

  checkCallback = true;

  requirejs.config({
    baseUrl: baseUrl,
    paths: {
      CustomInput: baseUrl,
      requirejs: '//cdn.rawgit.com/jrburke/requirejs/49086cf903410c746140fd968151a8aa95cc3482/',
      jquery: "//ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min"
    }
  });

  require(["CustomInput/CustomInput"], function(CustomInput) {
    if (loadGlobally) {
      window.CustomInput = CustomInput;
    }
    if (checkCallback && window.onCustomInput) {
      return window.onCustomInput(CustomInputs);
    }
  });

}).call(this);
